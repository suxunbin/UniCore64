From 342ea3ecc50b5d714b027a81e94d00735d10bdde Mon Sep 17 00:00:00 2001
From: Guan Xuetao <gxt@mprc.pku.edu.cn>
Date: Thu, 8 Dec 2011 10:20:35 +0800
Subject: [PATCH 122/641] UniCore64: modify cpu-ucv3.c to cpu-uc64.c

Signed-off-by: Guan Xuetao <gxt@mprc.pku.edu.cn>
---
 arch/unicore64/kernel/Makefile   |    2 +-
 arch/unicore64/kernel/cpu-uc64.c |   65 ++++++++++++++++++++++++++++++++++++++
 arch/unicore64/kernel/cpu-ucv3.c |   65 --------------------------------------
 3 files changed, 66 insertions(+), 66 deletions(-)
 create mode 100644 arch/unicore64/kernel/cpu-uc64.c
 delete mode 100644 arch/unicore64/kernel/cpu-ucv3.c

diff --git a/arch/unicore64/kernel/Makefile b/arch/unicore64/kernel/Makefile
index 239ef31..90386e9 100644
--- a/arch/unicore64/kernel/Makefile
+++ b/arch/unicore64/kernel/Makefile
@@ -1,4 +1,4 @@
 obj-y			:= ptrace.o setup.o init_task.o process.o
-obj-y			+= time.o traps.o irq.o cpu-ucv3.o sys.o entry.o
+obj-y			+= time.o traps.o irq.o cpu-uc64.o sys.o entry.o
 head-y			:= head.o
 extra-y			:= $(head-y) vmlinux.lds
diff --git a/arch/unicore64/kernel/cpu-uc64.c b/arch/unicore64/kernel/cpu-uc64.c
new file mode 100644
index 0000000..9681bd0
--- /dev/null
+++ b/arch/unicore64/kernel/cpu-uc64.c
@@ -0,0 +1,65 @@
+#include <linux/kernel.h>
+#include <linux/seq_file.h>
+
+/**
+ * cpu_ucv3_show() -
+ * @m:
+ * @v:
+ */
+static int cpu_ucv3_show(struct seq_file *m, void *v)
+{
+	/* FIXME */
+	BUG();
+	return 0;
+}
+
+/**
+ * cpu_ucv3_start() -
+ * @m:
+ * @pos:
+ */
+static void *cpu_ucv3_start(struct seq_file *m, loff_t *pos)
+{
+	/* FIXME */
+	BUG();
+}
+
+/**
+ * cpu_ucv3_next() -
+ * @v:
+ * @pos:
+ */
+static void *cpu_ucv3_next(struct seq_file *m, void *v, loff_t *pos)
+{
+	/* FIXME */
+	BUG();
+}
+
+/**
+ * cpu_ucv3_stop() -
+ * @m:
+ * @v:
+ */
+static void cpu_ucv3_stop(struct seq_file *m, void *v)
+{
+	/* FIXME */
+	BUG();
+}
+
+/**
+ * struct cpuinfo_op -
+ * @start:
+ * @next:
+ * @stop:
+ * @show:
+ */
+const struct seq_operations cpuinfo_op = {
+	.start	= cpu_ucv3_start,
+	.next	= cpu_ucv3_next,
+	.stop	= cpu_ucv3_stop,
+	.show	= cpu_ucv3_show
+};
+
+#ifdef CONFIG_CPU_FREQ
+	/* FIXME */
+#endif
diff --git a/arch/unicore64/kernel/cpu-ucv3.c b/arch/unicore64/kernel/cpu-ucv3.c
deleted file mode 100644
index 9681bd0..0000000
--- a/arch/unicore64/kernel/cpu-ucv3.c
+++ /dev/null
@@ -1,65 +0,0 @@
-#include <linux/kernel.h>
-#include <linux/seq_file.h>
-
-/**
- * cpu_ucv3_show() -
- * @m:
- * @v:
- */
-static int cpu_ucv3_show(struct seq_file *m, void *v)
-{
-	/* FIXME */
-	BUG();
-	return 0;
-}
-
-/**
- * cpu_ucv3_start() -
- * @m:
- * @pos:
- */
-static void *cpu_ucv3_start(struct seq_file *m, loff_t *pos)
-{
-	/* FIXME */
-	BUG();
-}
-
-/**
- * cpu_ucv3_next() -
- * @v:
- * @pos:
- */
-static void *cpu_ucv3_next(struct seq_file *m, void *v, loff_t *pos)
-{
-	/* FIXME */
-	BUG();
-}
-
-/**
- * cpu_ucv3_stop() -
- * @m:
- * @v:
- */
-static void cpu_ucv3_stop(struct seq_file *m, void *v)
-{
-	/* FIXME */
-	BUG();
-}
-
-/**
- * struct cpuinfo_op -
- * @start:
- * @next:
- * @stop:
- * @show:
- */
-const struct seq_operations cpuinfo_op = {
-	.start	= cpu_ucv3_start,
-	.next	= cpu_ucv3_next,
-	.stop	= cpu_ucv3_stop,
-	.show	= cpu_ucv3_show
-};
-
-#ifdef CONFIG_CPU_FREQ
-	/* FIXME */
-#endif
-- 
1.7.9.5

