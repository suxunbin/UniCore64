From cb446937b280b9515328d2e58ce214debe15d79b Mon Sep 17 00:00:00 2001
From: Chang Huaixin <changhuaixin@mprc.pku.edu.cn>
Date: Thu, 11 Oct 2012 11:41:38 +0800
Subject: [PATCH 436/641] UniCore64: Add empty smp functions

Signed-off-by: Chang Huaixin <changhuaixin@mprc.pku.edu.cn>
---
 arch/unicore64/kernel/Makefile |    1 +
 arch/unicore64/kernel/smp.c    |   61 ++++++++++++++++++++++++++++++++++++++++
 2 files changed, 62 insertions(+)
 create mode 100644 arch/unicore64/kernel/smp.c

diff --git a/arch/unicore64/kernel/Makefile b/arch/unicore64/kernel/Makefile
index 900a1bc..eb95009 100644
--- a/arch/unicore64/kernel/Makefile
+++ b/arch/unicore64/kernel/Makefile
@@ -17,5 +17,6 @@ obj-$(CONFIG_UNICORE64)		+= cpu-uc64.o
 obj-$(CONFIG_OF_EARLY_FLATTREE)	+= devtree.o
 obj-$(CONFIG_EARLY_PRINTK)	+= early_printk.o
 obj-$(CONFIG_ARCH_PUV3)		+= puv3-core.o puv3-intc.o
+obj-$(CONFIG_SMP)			+= smp.o
 
 extra-y				:= vmlinux.lds
diff --git a/arch/unicore64/kernel/smp.c b/arch/unicore64/kernel/smp.c
new file mode 100644
index 0000000..a0e78bd
--- /dev/null
+++ b/arch/unicore64/kernel/smp.c
@@ -0,0 +1,61 @@
+#include <linux/init.h>
+#include <linux/smp.h>
+
+void __init smp_prepare_cpus(unsigned int max_cpus)
+{
+	/* FIXME */
+	BUG();
+}
+
+void __devinit smp_prepare_boot_cpu(void)
+{
+	/* FIXME */
+	BUG();
+}
+
+void smp_send_reschedule(int cpu)
+{
+	/* FIXME */
+	BUG();
+}
+
+int smp_call_function(smp_call_func_t func, void *info, int wait)
+{
+	/* FIXME */
+	BUG();
+	return 0;
+}
+
+void smp_call_function_many(const struct cpumask *mask, smp_call_func_t func,
+				void *info, bool wait)
+{
+	/* FIXME */
+	BUG();
+}
+
+int smp_call_function_single(int cpuid, smp_call_func_t func, void *info,
+				int wait)
+{
+	/* FIXME */
+	BUG();
+	return 0;
+}
+
+void __init smp_cpus_done(unsigned int max_cpus)
+{
+	/* FIXME */
+	BUG();
+}
+
+int __cpuinit __cpu_up(unsigned int cpu, struct task_struct *tidle)
+{
+	/* FIXME */
+	BUG();
+	return 0;
+}
+
+void smp_send_stop(void)
+{
+	/* FIXME */
+	BUG();
+}
-- 
1.7.9.5

